apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  labels:
    build: art-build-failures
  name: art-build-failures
spec:
  lookupPolicy:
    local: false
  tags:
  - name: base
    annotations:
      description: Base image with all dependencies
  - name: latest
    annotations:
      description: Application image with code
