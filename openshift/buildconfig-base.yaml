apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  labels:
    build: art-build-failures-base
  name: art-build-failures-base
spec:
  failedBuildsHistoryLimit: 5
  nodeSelector: null
  output:
    to:
      kind: ImageStreamTag
      name: art-build-failures:base
  runPolicy: Serial
  source:
    git:
      uri: https://github.com/openshift-eng/art-dashboard-ui.git
      ref: art-build-failures
    type: Git
  strategy:
    dockerStrategy:
      dockerfilePath: docker/Dockerfile.base
      from:
        kind: DockerImage
        name: registry.redhat.io/ubi9/python-311
    type: Docker
  successfulBuildsHistoryLimit: 5
  triggers:
  - type: ConfigChange
  - imageChange: {}
    type: ImageChange
